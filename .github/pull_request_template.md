PR Checklist

- [ ] Base branch is `develop` (unless release/hotfix explicitly targets `master`)
- [ ] CI Smoke is green (linux-smoke + windows-smoke)
- [ ] package-lock.json is present and committed
- [ ] Changes are scoped and reviewed (small PR preferred)

Note: `master` is the default/release branch. `develop` is the day-to-day integration branch.
